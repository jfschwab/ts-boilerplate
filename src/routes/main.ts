// containers
import Test from '@components/Test';

// routes model
import { RoutesModel } from './model/RoutesModel';

// main routes
export const main: Array<RoutesModel> = [
  {
    path: '/',
    title: 'Home',
    content: Test
  }
]

export default main;